"""
Utils package — Shared helper functions.

This package contains small, reusable utilities that don't belong to any
specific AI model or pipeline.  They are pure Python functions with no
heavy dependencies (no torch, no whisper, no pyannote).

Modules:
    formatting : Timestamp conversion and text cleanup.
    audio      : Audio file validation and format detection.
"""
